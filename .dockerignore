# 前端源码和构建产物不进运行镜像上下文；二进制 bin/katch 必须保留，
# 由 CI 宿主环境构建后交给 Dockerfile 拷入最终镜像。
frontend/node_modules
frontend/dist
internal/web/dist

.git
data
runtime

# 开发者本地的配置（可能带真的管理密钥和 DSN）不进构建上下文；
# 镜像里那份由 Dockerfile 从 configs/config.yaml.example 拷。
configs/config.yaml
