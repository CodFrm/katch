# 这两个目录必须排除：宿主机上的构建产物会盖掉 web stage 干净构建的结果，
# 让镜像里混进本地残留的旧 chunk。
frontend/node_modules
frontend/dist
internal/web/dist

.git
bin
data
runtime

# 开发者本地的配置（可能带真的管理密钥和 DSN）不进构建上下文；
# 镜像里那份由 Dockerfile 从 configs/config.yaml.example 拷。
configs/config.yaml
